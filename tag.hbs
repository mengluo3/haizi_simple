{{!< default}}

<header id="blog-header"{{#if tag.feature_image}} class="has-cover"{{else}}{{#if @site.cover_image}} class="has-cover"{{/if}}{{/if}}>
	<div class="inner">
	</div>
</header>

<div id="index" class="container">
	

	<main class="content" role="main">
		<div style="padding-left:5%;">
			
			<span style="font-size:2em;">Tag: </span>
			<span style="font-size:2em; font-weight:1000;">{{tag.name}}</span>
			<i>
			<span class="blog-description">
				{{#if tag.description}}
					{{tag.description}}
				{{else}}
					{{t "Posts"}}: {{pagination.total}}
				{{/if}}
			</span>
			</i>
			<hr>
		</div>
		
		<br>

		{{> "loop"}}

	</main>

</div>
